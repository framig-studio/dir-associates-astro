export interface CTA {
  text: string;
  href: string;
}

export interface Header {
  text: string;
  boldText: string;
}

export interface TrustStat {
  num: string;
  label: string;
}

export interface FAQItem {
  question: string;
  answer: string;
}
